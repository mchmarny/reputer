name: 'Reputer'
description: 'Post contributor reputation scores on pull requests'

inputs:
  reputer-version:
    description: 'Reputer release version to install (e.g. v0.2.4)'
    required: false
    default: 'latest'
  github-token:
    description: 'GitHub token for API access and PR comments'
    required: false
    default: ${{ github.token }}
  score-green:
    description: 'Score >= this shows green (0-100)'
    required: false
    default: '70'
  score-yellow:
    description: 'Score >= this (but < green) shows yellow; below shows red (0-100)'
    required: false
    default: '40'
  trusted-orgs:
    description: 'Org names whose members get a scoring boost (one per line or comma-separated)'
    required: false
    default: ''

runs:
  using: 'composite'
  steps:

  - name: Install reputer
    shell: bash
    env:
      VERSION: ${{ inputs.reputer-version }}
    run: |
      set -euo pipefail

      if [ "${VERSION}" = "latest" ]; then
        VERSION=$(curl -sSL https://api.github.com/repos/mchmarny/reputer/releases/latest \
          | jq -r '.tag_name')
      fi

      # Strip leading v for artifact filename (goreleaser uses version without v)
      FILE_VERSION="${VERSION#v}"

      # Detect OS and architecture
      OS="$(uname -s | tr '[:upper:]' '[:lower:]')"
      RAW_ARCH="$(uname -m)"
      case "${RAW_ARCH}" in
        x86_64)        ARCH="amd64" ;;
        aarch64|arm64) ARCH="arm64" ;;
        *)             echo "Unsupported architecture: ${RAW_ARCH}" >&2; exit 1 ;;
      esac

      ARTIFACT="reputer_${FILE_VERSION}_${OS}_${ARCH}"
      URL="https://github.com/mchmarny/reputer/releases/download/${VERSION}/${ARTIFACT}"
      CHECKSUM_URL="https://github.com/mchmarny/reputer/releases/download/${VERSION}/checksums.txt"

      echo "Downloading reputer ${VERSION} from ${URL}"
      curl -sSL -o /usr/local/bin/reputer "${URL}"

      echo "Verifying checksum"
      curl -sSL -o /tmp/checksums.txt "${CHECKSUM_URL}"
      EXPECTED=$(grep "${ARTIFACT}" /tmp/checksums.txt | head -1 | awk '{print $1}')
      ACTUAL=$(sha256sum /usr/local/bin/reputer | awk '{print $1}')
      if [[ "${ACTUAL}" != "${EXPECTED}" ]]; then
        echo "::error::reputer checksum mismatch: expected ${EXPECTED}, got ${ACTUAL}"
        exit 1
      fi

      chmod +x /usr/local/bin/reputer
      reputer --version

  - name: Run reputer
    id: reputer
    shell: bash
    env:
      GITHUB_TOKEN: ${{ inputs.github-token }}
    run: |
      set -euo pipefail
      REPO="github.com/${{ github.repository }}"
      ARGS="--repo ${REPO} --stats --file /tmp/report.json"
      while IFS= read -r org; do
        org=$(echo "${org}" | xargs)
        [ -n "${org}" ] && ARGS="${ARGS} --trusted-orgs ${org}"
      done < <(echo "${{ inputs.trusted-orgs }}" | tr ',' '\n')
      reputer ${ARGS} || true
      if [ -f /tmp/report.json ]; then
        echo "has_report=true" >> "$GITHUB_OUTPUT"
      else
        echo "has_report=false" >> "$GITHUB_OUTPUT"
      fi

  - name: Post reputation comment
    if: steps.reputer.outputs.has_report == 'true'
    uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3  # v9.0.0
    env:
      SCORE_GREEN: ${{ inputs.score-green }}
      SCORE_YELLOW: ${{ inputs.score-yellow }}
    with:
      script: |
        const fs = require('fs');
        const author = context.payload.pull_request.user.login;

        let report;
        try {
          report = JSON.parse(fs.readFileSync('/tmp/report.json', 'utf8'));
        } catch (e) {
          core.warning(`Failed to parse reputer report: ${e.message}`);
          return;
        }

        const contributor = (report.contributors || []).find(
          c => c.username && c.username.toLowerCase() === author.toLowerCase()
        );

        if (!contributor || !contributor.stats) {
          core.info(`No reputation data found for ${author}; skipping comment.`);
          return;
        }

        const s = contributor.stats;
        const score = (contributor.reputation * 100).toFixed(1);

        const greenThreshold = Number(process.env.SCORE_GREEN) || 70;
        const yellowThreshold = Number(process.env.SCORE_YELLOW) || 40;
        const scoreNum = parseFloat(score);
        const icon = scoreNum >= greenThreshold ? '\u{1F7E2}' : scoreNum >= yellowThreshold ? '\u{1F7E1}' : '\u{1F534}';

        const rows = [
          `| Author Association | ${s.author_association || 'NONE'} |`,
          `| Trusted Org Member | ${s.trusted_org_member ? 'Yes' : 'No'} |`,
          `| Account Age | ${Number(s.age_days).toLocaleString()} days |`,
          `| Verified Commits | ${s.commits - s.unverified_commits}/${s.commits} |`,
          `| PRs Merged | ${Number(s.prs_merged || 0).toLocaleString()} |`,
          `| PRs Closed | ${Number(s.prs_closed || 0).toLocaleString()} |`,
          `| Public Repos | ${Number(s.public_repos || 0).toLocaleString()} |`,
          `| Forked Repos | ${Number(s.forked_repos || 0).toLocaleString()} |`,
          `| Followers | ${Number(s.followers || 0).toLocaleString()} |`,
          `| Recent PR Repos | ${Number(s.recent_pr_repo_count || 0).toLocaleString()} |`,
        ];

        const body = [
          '### Contributor Reputation',
          '',
          `${icon} **Score: ${score}%**`,
          '',
          '| Metric | Value |',
          '|--------|-------|',
          ...rows,
          '',
          '*Powered by [reputer](https://github.com/mchmarny/reputer)*',
        ].join('\n');

        await github.rest.issues.createComment({
          owner: context.repo.owner,
          repo: context.repo.repo,
          issue_number: context.payload.pull_request.number,
          body,
        });

        core.info('Reputation comment posted successfully.');
