// Package logging provides structured logging utilities for reputer.
//
// Three logging modes: CLI (user-friendly), text (debug), JSON (structured).
// All modes write to stderr and use log/slog under the hood.
package logging
