// Package cli defines the reputer command-line interface.
//
// It handles flag parsing, logging configuration, and orchestrates
// the reputation report workflow. Build-time variables (version,
// commit, date) are injected via ldflags.
package cli
